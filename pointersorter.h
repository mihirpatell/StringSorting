
#ifndef pointersorter_h
#define pointersorter_h

#include <stdio.h>

typedef struct _node{ //declare node object with two fields, data and next node
    char* data;
    struct _node* next;
}node;

#endif /* pointersorter_h */
